# Copy to keystore.properties (gitignored) and fill in real values for
# local release builds. CI uses the matching env vars KEYSTORE_FILE /
# KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD instead.
storeFile=../release.jks
storePassword=changeme
keyAlias=focusedreader
keyPassword=changeme
